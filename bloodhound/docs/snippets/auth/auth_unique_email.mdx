---
title: "Auth Unique Email"
description: "BloodHound documentation"
icon: "droplet"
---

<Warning>
{auth_mode} users must have an email address that is **unique** across all authentication methods (built-in, OIDC, SAML). Account creation fails if a duplicate email is detected.

  <img
    src="/images/sso/create_user_fail_email.png"
    alt="A view of the error message shown when attempting to create a user with a duplicate email address."
  />

</Warning>
