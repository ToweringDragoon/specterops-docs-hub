import { RefractorElement } from 'refractor';
import type { Plugin } from 'unified';
import type { Node } from 'unist';
import { Parent } from 'unist';
export type RehypeSyntaxHighlightingOptions = {
    ignoreMissing?: boolean;
    alias?: Record<string, string[]>;
};
export type TreeNode = RefractorElement & Node & {
    properties: {
        className?: string[];
    };
};
export type TreeParent = Parent & {
    tagName: string;
    properties: {
        className?: string[];
    };
};
export declare const rehypeSyntaxHighlighting: Plugin<[RehypeSyntaxHighlightingOptions?], TreeNode>;
